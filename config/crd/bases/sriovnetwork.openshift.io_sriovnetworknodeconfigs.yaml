
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.3.0
  creationTimestamp: null
  name: sriovnetworknodeconfigs.sriovnetwork.openshift.io
spec:
  group: sriovnetwork.openshift.io
  names:
    kind: SriovNetworkNodeConfig
    listKind: SriovNetworkNodeConfigList
    plural: sriovnetworknodeconfigs
    singular: sriovnetworknodeconfig
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: SriovNetworkNodeConfig is the Schema for the sriovnetworknodeconfigs
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: SriovNetworkNodeConfigSpec defines the desired state of SriovNetworkNodeConfig
            properties:
              ovsHardwareOffload:
                description: OvsHardwareOffload describes the OVS HWOL configuration
                  for selected Nodes
                items:
                  properties:
                    nodeSelector:
                      additionalProperties:
                        type: string
                      description: 'On Kubernetes: NodeSelector selects Kubernetes
                        Nodes to be configured with OVS HWOL configurations OVS HWOL
                        configurations are generated automatically by Operator Labels
                        in NodeSelector are ANDed when selecting Kubernetes Nodes
                        On OpenShift: NodeSelector matches on Labels defined in MachineConfigPoolSpec.NodeSelector
                        OVS HWOL MachineConfigs are generated and applied to Nodes
                        in MachineConfigPool Labels in NodeSelector are ANDed when
                        matching on MachineConfigPoolSpec.NodeSelector'
                      type: object
                  type: object
                type: array
              rdmaExclusiveMode:
                description: RDMAExclusiveMode enables RDMA exclusive mode for the
                  selected nodes
                items:
                  properties:
                    nodeSelector:
                      additionalProperties:
                        type: string
                      type: object
                  type: object
                type: array
            type: object
          status:
            description: SriovNetworkNodeConfigStatus defines the observed state of
              SriovNetworkNodeConfig
            properties:
              ovsHardwareOffload:
                description: Show the runtime status of OvsHardwareOffload
                items:
                  properties:
                    nodes:
                      description: 'On Kubernetes: Nodes shows the selected names
                        of Kubernetes Nodes that are configured with OVS HWOL On OpenShift:
                        Nodes shows the selected names of MachineConfigPools that
                        are configured with OVS HWOL'
                      items:
                        type: string
                      type: array
                  type: object
                type: array
              rdmaExclusiveMode:
                items:
                  properties:
                    nodes:
                      items:
                        type: string
                      type: array
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
